import React from "react";
import { createSlice } from "@reduxjs/toolkit";

function Cart() {
    return ( <
        div className = "Cart" >
        <
        h2 > carts < /h2>{" "} <
        /div>
    );
}

export default Cart;